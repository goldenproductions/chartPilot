apiVersion: v1
kind: Service
metadata:
  name: {{ include "fixture-api.name" . }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    {{- include "fixture-api.labels" . | nindent 4 }}
