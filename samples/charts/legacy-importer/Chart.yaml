apiVersion: v2
name: legacy-importer
description: Nightly member data importer, lifted from the old VM platform. Deliberately bad — this is the chart ChartPilot is demonstrated against.
type: application
version: 0.9.0
appVersion: "latest"

dependencies:
  - name: legacy-cache
    version: "^1.0.0"
    repository: https://charts.example.com/legacy
